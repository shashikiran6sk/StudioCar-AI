/** Where a photo in the Selection Dialog came from. */
export enum PhotoSource {
  /** An original stored by an earlier batch of this vehicle. */
  Existing = "EXISTING",
  /** A file chosen in this dialog, uploaded straight to private storage. */
  Upload = "UPLOAD",
}
