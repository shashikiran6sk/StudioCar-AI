# AWS infrastructure

Infrastructure as code for private S3 storage, SQS queues, dead-letter queues, workers, IAM, and alarms will be introduced with the upload and processing increments.

This directory intentionally contains no deployable resources in the repository-foundation increment.

