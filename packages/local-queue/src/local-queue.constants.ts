export const DEFAULT_WAIT_TIME_SECONDS = 20;
export const DEFAULT_MAX_MESSAGES = 10;
export const DEFAULT_IDLE_DELAY_MS = 250;
export const DEFAULT_ERROR_DELAY_MS = 2_000;

export const MAXIMUM_WAIT_TIME_SECONDS = 20;
export const MAXIMUM_MAX_MESSAGES = 10;

export const INVALID_CONSUMER_OPTIONS_MESSAGE =
  "Local queue consumer options must stay within the SQS receive limits.";
